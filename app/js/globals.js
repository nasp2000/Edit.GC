// ── Globals used across modules ───────────────────────────────
let originMarkMode = null; // 'left' | 'right' | null for mark placement

const previewOpts = {
  showBounds: true,
  colorByFeed: false,
  compareMode: false,
  showMinimap: true,
};
